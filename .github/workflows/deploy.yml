name: Deploy to VPS

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Stop app on VPS
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          # Single line: kill node.exe+children, wait ~3s for Windows to release file handles, then force exit 0
          script: taskkill /F /T /IM node.exe 2>nul & ping -n 4 127.0.0.1 >nul 2>&1 & exit 0

      - name: Pull latest code on VPS
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            cd /d "D:\MDF WEBSITE\Website" && git fetch origin main && git reset --hard origin/main

      - name: Start app on VPS
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            cd /d "D:\MDF WEBSITE\Website" && start /B node dist\index.cjs > "D:\MDF WEBSITE\Website\app.log" 2>&1
